import { MainContainer } from "../styles/mainpage";
import NoticeBoard from "../components/mainpage/NoticeBoard";

const Home = () => {
  return (
    <MainContainer>
      <NoticeBoard />
    </MainContainer>
  );
};

export default Home;

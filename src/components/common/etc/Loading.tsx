const Loading = () => {
  return (
    <div
      style={{
        width: "100vw",
        height: "300px",
        display: "flex",
        fontSize: "100px",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      ...
    </div>
  );
};

export default Loading;

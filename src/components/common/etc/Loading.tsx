const Loading = () => {
  return (
    <div
      style={{
        width: "300px",
        height: "300px",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      ...
    </div>
  );
};

export default Loading;
